"use client";

// PageRefreshHandler is now a no-op component
// Tab switching and navigation should work naturally without forced refreshes
export function PageRefreshHandler() {
  return null; // This component no longer does anything
}
